#!/usr/bin/env bash
# This script displays "Best School" 10 times using an until loop

# Initialize counter
counter=0

# Loop until counter is greater than or equal to 10
until [ "$counter" -ge 10 ]; do
    echo "Best School"
    ((counter++))
done
